const toggle=document.querySelector('.menu-toggle'),menu=document.querySelector('#menu');
function closeMenu(){menu.classList.remove('open');toggle.setAttribute('aria-expanded','false');toggle.setAttribute('aria-label','Abrir menú')}
toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')!=='true';menu.classList.toggle('open',open);toggle.setAttribute('aria-expanded',String(open));toggle.setAttribute('aria-label',open?'Cerrar menú':'Abrir menú')});
menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));document.addEventListener('keydown',e=>{if(e.key==='Escape'){closeMenu();toggle.focus()}});
const route=location.pathname.split('/').filter(Boolean).pop()||'index';const current=route.endsWith('.html')?route:route+'.html';menu.querySelectorAll('a').forEach(a=>{if(a.getAttribute('href')===current)a.setAttribute('aria-current','page')});document.querySelector('#year').textContent=new Date().getFullYear();
if('IntersectionObserver' in window&&!matchMedia('(prefers-reduced-motion: reduce)').matches){document.documentElement.classList.add('js-motion');const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el))}
const form=document.querySelector('#quote-form');if(form){const select=document.querySelector('#service-select'),value=new URLSearchParams(location.search).get('servicio');if([...select.options].some(o=>o.value===value))select.value=value;function message(){const d=new FormData(form);return ['Solicitud de cotización — RELIEVE','Nombre: '+d.get('nombre'),'Empresa: '+(d.get('empresa')||'No indicada'),'Correo: '+d.get('email'),'Teléfono: '+d.get('telefono'),'Servicio: '+select.options[select.selectedIndex].text,'Ubicación: '+d.get('ubicacion'),'Proyecto: '+d.get('mensaje')].join('\n')};form.addEventListener('submit',e=>{e.preventDefault();if(!form.reportValidity())return;window.open('https://wa.me/'+window.RELIEVE_CONTACT.telefono+'?text='+encodeURIComponent(message()),'_blank','noopener,noreferrer');document.querySelector('#form-status').textContent='Tu solicitud está preparada. Completa el envío en WhatsApp.'});document.querySelector('#email-quote').addEventListener('click',()=>{if(!form.reportValidity())return;location.href='mailto:'+window.RELIEVE_CONTACT.email+'?subject='+encodeURIComponent('Solicitud de cotización — RELIEVE')+'&body='+encodeURIComponent(message());document.querySelector('#form-status').textContent='Revisa y envía la solicitud desde tu aplicación de correo.'})}
